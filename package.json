{
  "name": "getting-started-with-netlify-edge-functions",
  "version": "1.0.0",
  "description": "Code examples to support the Getting Started with Netlify Edge Functions tutorial.",
  "scripts": {
    "build": "esbuild --bundle netlify/edge-functions/record-command.js --outfile=netlify/functions/record-command.js"
  },

  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}
